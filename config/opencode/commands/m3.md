---
description: Implement a task with MiniMax M3
agent: 02-m3-build
---

Implement $ARGUMENTS.

First inspect the relevant existing code and conventions. Make the smallest coherent change that solves the problem. Run the relevant tests or checks after editing. Do not stop at a plan unless blocked.
