# dotfiles

Personal configuration files, synced across machines.

## Usage

Clone into your home directory (or symlink individual files) and edit as needed.

```sh
git clone https://github.com/jeidsath/dotfiles.git ~/dotfiles
```
